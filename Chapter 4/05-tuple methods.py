# Because tuples are immutable so it has only two built-in functions ".count()" and ".index()"
# so, there are very few tuple methods 

a = ("waqas", 45.9, 1, 2)

print(a.count("waqas")) # counts elements

a = ("waqas", 45.9, 1, 2)
print(a.index(2)) # finds the index of value